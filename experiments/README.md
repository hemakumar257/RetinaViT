# Experiments

This folder contains guidelines and logs for experiment tracking.

## Naming Convention
`YYYYMMDD_feature_name_variant` (e.g., `20260211_vit_base_adamw`)

## Logging
Always enable logging to `wandb` or `tensorboard` as configured in the `base.yaml`.
